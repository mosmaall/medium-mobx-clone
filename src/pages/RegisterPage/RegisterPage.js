import React from 'react'
import RegisterForm from './RegisterPage.form'
import Form from 'antd'

const RegisterPage = compose((props: Props) => {
  console.log('RegisterProps =', props)
  return (
    <div>
      <p>RegisterPage</p>
    </div>
  )
})

export default RegisterPage
